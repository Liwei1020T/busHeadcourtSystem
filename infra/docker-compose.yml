services:
  # PostgreSQL Database
  db:
    image: postgres:15-alpine
    container_name: bus-optimizer-db
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      POSTGRES_DB: ${POSTGRES_DB:-bus_optimizer}
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ../backend/init_postgres.sql:/docker-entrypoint-initdb.d/init.sql:ro
    ports:
      - "${DB_PORT:-5432}:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres}"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - bus-optimizer-network

  # Backend API
  backend:
    build:
      context: ../backend
      dockerfile: ../infra/Dockerfile.backend
    container_name: bus-optimizer-api
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD:-postgres}@db:5432/${POSTGRES_DB:-bus_optimizer}
      API_KEYS: ${API_KEYS:-ENTRY_GATE:ENTRY_SECRET}
      DEBUG: ${DEBUG:-false}
    ports:
      - "${BACKEND_PORT:-8000}:8000"
    depends_on:
      db:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - bus-optimizer-network

  # Web Dashboard
  web:
    build:
      context: ../web-dashboard
      dockerfile: ../infra/Dockerfile.web
    container_name: bus-optimizer-web
    ports:
      - "${WEB_PORT:-5175}:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - backend
    restart: unless-stopped
    networks:
      - bus-optimizer-network

  # Pi Agent (Optional - for containerized deployment)
  pi-agent:
    build:
      context: ..
      dockerfile: ./infra/Dockerfile.pi-agent
    container_name: bus-optimizer-pi-agent
    environment:
      API_BASE_URL: ${PI_API_BASE_URL:-http://backend:8000/api/bus}
      API_KEY: ${PI_API_KEY:-ENTRY_SECRET}
      UPLOAD_INTERVAL: ${PI_UPLOAD_INTERVAL:-60}
    volumes:
      - pi_agent_data:/app/data
    depends_on:
      - backend
    restart: unless-stopped
    networks:
      - bus-optimizer-network
    profiles:
      - pi-agent  # Only start if explicitly requested

volumes:
  postgres_data:
    driver: local
  pi_agent_data:
    driver: local

networks:
  bus-optimizer-network:
    driver: bridge

